{% extends 'base.html.twig' %}

{% block title %}Knowledge Learning - Accueil{% endblock %}

{% block body %}
    <div class="container">
        <h1>Bienvenue sur Knowledge Learning</h1>
        
        <div class="themes-grid">
            {% for theme in themes %}
                <div class="theme-card">
                    <h2>{{ theme.name }}</h2>
                    <p>{{ theme.description }}</p>
                    <div class="cursus-count">
                        {{ theme.cursus|length }} cursus disponibles
                    </div>
                    <a href="{{ path('app_theme_show', {'id': theme.id}) }}" 
                       class="btn btn-primary">
                        Découvrir
                    </a>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
