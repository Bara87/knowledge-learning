<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Knowledge Learning{% endblock %}</title>
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}">
        <link href="https://fonts.googleapis.com/css2?family=Comic+Sans+MS&display=swap" rel="stylesheet">
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% block stylesheets %}
            <style>
                :root {
                    /* Palette de couleurs principale */
                    --color-light: #f1f8fc;    /* Bleu très clair */
                    --color-primary: #0074c7;  /* Bleu moyen */
                    --color-dark: #00497c;     /* Bleu foncé */
                    --color-neutral: #384050;  /* Gris bleuté */
                    
                    /* Couleurs d'accentuation (à utiliser avec parcimonie) */
                    --color-accent-red: #cd2c2e;    /* Rouge */
                    --color-accent-green: #82b864;  /* Vert */
                }

                body {
                    font-family: 'Comic Sans MS', cursive, sans-serif;
                    color: var(--color-neutral);
                    background-color: var(--color-light);
                }

                /* Boutons principaux */
                .btn-primary {
                    background-color: var(--color-primary);
                    border-color: var(--color-primary);
                }
                .btn-primary:hover {
                    background-color: var(--color-dark);
                    border-color: var(--color-dark);
                }

                /* Liens */
                a {
                    color: var(--color-primary);
                }
                a:hover {
                    color: var(--color-dark);
                }

                /* Badges et alertes */
                .badge.bg-primary {
                    background-color: var(--color-primary) !important;
                }
                .badge.bg-secondary {
                    background-color: var(--color-neutral) !important;
                }
                
                /* Alertes de succès/erreur */
                .alert-success {
                    background-color: var(--color-accent-green); /* #82b864 */
                    border-color: var(--color-accent-green);
                    color: white;
                }
                .alert-danger {
                    background-color: var(--color-accent-red); /* #cd2c2e */
                    border-color: var(--color-accent-red);
                    color: white;
                }

                /* Headers et titres */
                h1, h2, h3, h4, h5, h6 {
                    color: var(--color-dark);
                }

                /* Progress bars */
                .progress-bar {
                    background-color: var(--color-primary); /* #0074c7 */
                }

                /* Navigation */
                .nav-link {
                    color: var(--color-neutral) !important; /* #384050 */
                }
                
                /* Titres */
                h1, h2, h3, h4, h5, h6 {
                    color: var(--color-dark); /* #00497c */
                }
                
                /* Boutons */
                .btn-primary {
                    background-color: var(--color-primary); /* #0074c7 */
                    border-color: var(--color-primary);
                }
                .btn-primary:hover {
                    background-color: var(--color-dark); /* #00497c */
                    border-color: var(--color-dark);
                }

                /* Footer */
                .text-muted {
                    color: var(--color-neutral) !important; /* #384050 pour la description */
                }
                
                /* Icônes réseaux sociaux */
                .fab {
                    color: var(--color-primary); /* #0074c7 */
                }
                .fab:hover {
                    color: var(--color-dark); /* #00497c */
                }
                
                /* Icônes contact */
                .fas {
                    color: var(--color-primary); /* #0074c7 */
                }
                
                /* Mentions légales */
                .footer-bottom a {
                    color: var(--color-primary); /* #0074c7 */
                }
                .footer-bottom a:hover {
                    color: var(--color-dark); /* #00497c */
                }
                
                /* Prix et badges */
                .badge {
                    background-color: var(--color-primary); /* #0074c7 */
                }
                
                /* Séparateurs */
                hr {
                    border-color: var(--color-neutral); /* #384050 */
                    opacity: 0.2;
                }

                /* Liens de navigation actifs */
                .nav-link.active {
                    color: var(--color-primary) !important; /* #0074c7 */
                    font-weight: bold;
                }

                /* Formulaires */
                .form-control:focus {
                    border-color: var(--color-primary); /* #0074c7 */
                    box-shadow: 0 0 0 0.25rem rgba(0, 116, 199, 0.25);
                }

                /* Tableaux */
                .table thead th {
                    color: var(--color-dark); /* #00497c */
                }
                
                /* Cards */
                .card {
                    border-color: rgba(56, 64, 80, 0.1); /* #384050 avec opacité */
                }
                .card-header {
                    background-color: var(--color-light); /* #f1f8fc */
                }
                
                /* Dropdown menus */
                .dropdown-item:active {
                    background-color: var(--color-primary); /* #0074c7 */
                }
                .dropdown-item:hover {
                    background-color: var(--color-light); /* #f1f8fc */
                }
                
                /* Tooltips */
                .tooltip .tooltip-inner {
                    background-color: var(--color-neutral); /* #384050 */
                }

                /* Navbar toggler (bouton hamburger) */
                .navbar-toggler {
                    border-color: var(--color-neutral);
                }
                .navbar-toggler:focus {
                    box-shadow: 0 0 0 0.25rem rgba(56, 64, 80, 0.25);
                }
                
                /* Liens de pagination */
                .page-link {
                    color: var(--color-primary);
                }
                .page-link:hover {
                    color: var(--color-dark);
                }
                .page-item.active .page-link {
                    background-color: var(--color-primary);
                    border-color: var(--color-primary);
                }
                
                /* Listes à puces personnalisées */
                .list-unstyled li::before {
                    color: var(--color-primary);
                }
                
                /* Focus outline pour accessibilité */
                *:focus {
                    outline-color: var(--color-primary);
                }
            </style>
        {% endblock %}
    </head>
    <body>
        <header>
            <nav class="navbar navbar-expand-lg" style="background-color: #FFFFFF">
                <div class="container">
                    <a class="navbar-brand" href="{{ path('app_home') }}">
                        <img src="{{ asset('images/logo.png') }}?v=2" alt="Knowledge Learning" width="100" style="height: auto;">
                    </a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav ms-auto">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                            </li>
                            {% if app.user %}
                                <li class="nav-item">
                                    <a class="nav-link" 
                                       href="{{ path('app_certification_index') }}" 
                                       data-bs-toggle="tooltip" 
                                       data-bs-placement="bottom" 
                                       title="Voir toutes vos certifications obtenues">
                                        Mes certifications
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_profile') }}">Mon profil</a>
                                </li>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <li class="nav-item">
                                        <a class="nav-link" href="{{ path('app_admin_dashboard') }}">Administration</a>
                                    </li>
                                {% endif %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                                </li>
                            {% else %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </nav>
        </header>

        <main class="py-4">
            <div class="container">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label }} alert-dismissible fade show">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    {% endfor %}
                {% endfor %}

                {% block body %}{% endblock %}
            </div>
        </main>

        <footer class="py-5 mt-auto" style="background-color: #FFFFFF">
            <div class="container">
                <div class="row">
                    <!-- À propos -->
                    <div class="col-md-4 mb-4">
                        <img src="{{ asset('images/logo.png') }}" alt="Knowledge Learning" width="150" class="mt-2">
                        <p class="text-muted">Plateforme innovante dédiée à l'apprentissage et à la certification des compétences professionnelles.</p>
                    </div>

                    <!-- Liens rapides -->
                    <div class="col-md-4 mb-4 d-flex justify-content-center">
                        <div>
                            <h5 class="mb-3">Liens rapides</h5>
                            <ul class="list-unstyled">
                                <li class="mb-2"><a href="{{ path('app_home') }}" class="text-decoration-none">Accueil</a></li>
                                <li class="mb-2"><a href="#" class="text-decoration-none">Nos formations</a></li>
                                <li class="mb-2"><a href="#" class="text-decoration-none">Centre d'aide</a></li>
                                <li class="mb-2"><a href="#" class="text-decoration-none">FAQ</a></li>
                                <li class="mb-2"><a href="#" class="text-decoration-none">Contact</a></li>
                            </ul>
                        </div>
                    </div>

                    <!-- Contact et réseaux sociaux -->
                    <div class="col-md-4 mb-4">
                        <h5 class="mb-3">Contactez-nous</h5>
                        <ul class="list-unstyled">
                            <li class="mb-2">
                                <i class="fas fa-map-marker-alt me-2" 
                                   data-bs-toggle="tooltip" 
                                   data-bs-placement="right" 
                                   title="Venez nous rencontrer"></i>
                                123 rue de l'Innovation, 75000 Paris
                            </li>
                            <li class="mb-2">
                                <i class="fas fa-phone me-2" 
                                   data-bs-toggle="tooltip" 
                                   data-bs-placement="right" 
                                   title="Appelez-nous du lundi au vendredi, 9h-18h"></i>
                                01 23 45 67 89
                            </li>
                            <li class="mb-2">
                                <i class="fas fa-envelope me-2"
                                   data-bs-toggle="tooltip" 
                                   data-bs-placement="right" 
                                   title="Nous vous répondons sous 24h"></i>
                                contact@knowledge-learning.fr
                            </li>
                        </ul>
                        <div class="mt-3">
                            <a href="#" class="text-decoration-none me-3" 
                               data-bs-toggle="tooltip" 
                               data-bs-placement="top" 
                               title="Suivez-nous sur Facebook">
                                <i class="fab fa-facebook-f"></i>
                            </a>
                            <a href="#" class="text-decoration-none me-3"
                               data-bs-toggle="tooltip" 
                               data-bs-placement="top" 
                               title="Suivez-nous sur Twitter">
                                <i class="fab fa-twitter"></i>
                            </a>
                            <a href="#" class="text-decoration-none me-3"
                               data-bs-toggle="tooltip" 
                               data-bs-placement="top" 
                               title="Suivez-nous sur LinkedIn">
                                <i class="fab fa-linkedin-in"></i>
                            </a>
                            <a href="#" class="text-decoration-none"
                               data-bs-toggle="tooltip" 
                               data-bs-placement="top" 
                               title="Suivez-nous sur Instagram">
                                <i class="fab fa-instagram"></i>
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Barre de séparation -->
                <hr class="my-4">

                <!-- Mentions légales -->
                <div class="row">
                    <div class="col-md-6 text-center text-md-start">
                        <p class="mb-0">&copy; {{ "now"|date("Y") }} Knowledge Learning. Tous droits réservés.</p>
                    </div>
                    <div class="col-md-6 text-center text-md-end">
                        <a href="#" class="text-decoration-none me-3">Mentions légales</a>
                        <a href="#" class="text-decoration-none me-3">CGU</a>
                        <a href="#" class="text-decoration-none">Politique de confidentialité</a>
                    </div>
                </div>
            </div>
        </footer>

        <!-- Bootstrap Bundle with Popper -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        
        {% block javascripts %}{% endblock %}

        <!-- Initialisation des tooltips -->
        <script>
            document.addEventListener('DOMContentLoaded', function() {
                var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
                var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                    return new bootstrap.Tooltip(tooltipTriggerEl)
                })
            });
        </script>

        <!-- Font Awesome pour les icônes -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    </body>
</html>